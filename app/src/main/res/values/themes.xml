<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Ana tema tanımı -->
    <style name="Theme.AltinGunu" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash screen tema tanımı -->
    <style name="Theme.AltinGunu.SplashScreen" parent="Theme.SplashScreen">
        <item name="android:windowIsTranslucent">false</item>
        <item name="windowSplashScreenBackground">@color/navy_blue</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash</item>
        <item name="windowSplashScreenAnimationDuration">300</item>
        <item name="postSplashScreenTheme">@style/Theme.AltinGunu</item>
    </style>
</resources>